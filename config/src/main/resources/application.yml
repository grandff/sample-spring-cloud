server:
  port: 8888

spring:
  application:
    name: config-service    
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
         search-locations: file:///${user.home}/workspace/cloud-sample-config # Windows
#      search-locations: file://${user.home}/workspace/cloud-sample-config # MacOS, linux
#     search-locations: classpath:/server-configs # local folder