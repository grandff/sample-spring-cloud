spring:
  cloud:
    config:
      uri: http://localhost:8888
      name: user-service
      profile: test
  
# config server actuator
management:
  endpoints:
    web:
      exposure:
        include: refresh  # refresh endpoint 활성화              